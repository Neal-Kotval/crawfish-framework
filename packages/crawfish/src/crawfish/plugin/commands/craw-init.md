---
name: crawfish-init
description: Scaffold a craw code project and start the ledger (writes files — user-invoked).
disable-model-invocation: true
allowed-tools: Bash
---

Run `craw code init $ARGUMENTS --json` and report the scaffolded files, the installed
plugin, and the next steps. This wrapper adds no logic — the CLI is the one execution path.
